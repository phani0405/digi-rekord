import { DistrictFilterPipe } from './district-filter.pipe';

describe('DistrictFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DistrictFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
